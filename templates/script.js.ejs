(function () {
    var module = { exports: {} },
        exports = module.exports,
        require = <%= namespace %>.req('<%= dirname %>');
<% script.split(/\n/).forEach(function (line) { %>
    <%- line %><% }); %>

    <%= namespace %>.modules['<%= modName %>'] = module.exports;
})();