(function () {
    var module = { exports: {} },
        exports = module.exports,
        require = _embedRequire.req('<%= cwd %>');
<% script.split(/\n/).forEach(function (line) { %>
    <%- line %><% }); %>

    _embedRequire.modules['<%= modName %>'] = module.exports;
})();